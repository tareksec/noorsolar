export default function Loading() {
  // Return null to prevent full-page blocking loader splash during navigation.
  // Meaningful content streams in directly while component-level loaders handle data.
  return null;
}
